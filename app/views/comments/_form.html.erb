<%= form_with url: post_comments_path(@post.id), scope: :data do |f| %>
    <%= f.text_field :text, { class: "form-control", placeholder: "Add Your Comment" } %>
    <%= f.submit "Comment", class: "btn btn-success my-2" %>
<% end %>
